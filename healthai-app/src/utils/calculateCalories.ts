export function calculateTotalCalories(_meals: unknown[]): number {
  return 0;
}

export function calculateMacros(_meals: unknown[]) {
  return { proteins: 0, carbs: 0, fats: 0 };
}

export function calculateCaloriesBurned(_workout: unknown): number {
  return 0;
}
