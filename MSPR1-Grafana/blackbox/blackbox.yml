modules:
  http_2xx:
    prober: http
    timeout: 10s
    http:
      method: GET
      valid_http_versions: ["HTTP/1.1", "HTTP/2.0"]
      follow_redirects: true
      fail_if_ssl: false
      # Les cibles sont des URLs publiques Railway -> IPv4 sur Internet.
      preferred_ip_protocol: "ip4"
