version: "3.8"

services:

  grafana:
    image: grafana/grafana
    container_name: health_grafana
    ports:
      - "3000:3000"
    env_file:
      - .env
    volumes:
      - grafana-storage:/var/lib/grafana

volumes:
  grafana-storage: